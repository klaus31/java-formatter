package x.java;

public class A {
    public A() {
        switch (b) {
            case AA:
                doIt();
        }
        switch (b) {
            case AA:
                doIt();
                break;
            case BB:
            case DD:
                for (int doIt : doIts) {
                    doIt(doIt);
                }
                break;
            default:
                doNot();
        }
        // WTF 1
        switch (getText()) {
        }
        // WTF 2
        switch (getText()) {
            default:;
        }
    }
}