package javaformatter.testfiles.java;
public class FooBar {
public String foo() {
return lines.stream()
.collect(toList());
}
public String foo() {
return lines.stream()
.filter(oOo::oOo)
.collect(toList());
}
}